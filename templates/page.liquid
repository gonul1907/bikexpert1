<section class="bx-content-page">
	<div class="bx-content-page__bg"></div>

	<div class="bx-content-page__shell">
		<header class="bx-content-page__header">
			<p class="bx-content-page__eyebrow">Informatie</p>
			<h1 class="bx-content-page__title">{{ page.title }}</h1>
		</header>

		<article class="bx-content-page__card rte">
			{{ page.content }}
		</article>
	</div>
</section>
